const formatoNumero = (num) => {
  return num.toString().slice(0,4);
}

const formatoNumeronuevo = (num) => {
  return num.toString().slice(0,4);
}

export {
    formatoNumero,
    formatoNumeronuevo
}